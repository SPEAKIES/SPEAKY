<!doctype html>
<html lang="ko">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="./css/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  <style>
    .posts_link {
      text-decoration: none;
      color: black;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1 class="mt-4">자유 게시판 - 자세히보기</h1>

    <div style="display: flex; justify-content:right;">
      <button type="submit" class="btn btn-primary mt-3 mb-3" onclick="location.href='/freeBoard/write'">새글 쓰기</button>
    </div>

    <% if(POST) { %>
    <div class="card mt-3" style="width: 100%">
      <div class="card-body">
        <div>
          <div>
            <h5 class="card-title mb-3"><%= POST.title %></h5>
            <h5 class="card-title mb-3"><%= POST.content %></h5>
            <%if(POST.img.length !== 0){%>
            <%for(let i = 0; i < POST.img.length; i++){%>
            <img src="../../uploads/<%=POST.img[i]%>" width="500" alt="test" />
            <%}%>
              <%}else{%>
            <%console.log('없음')%>
            <%}%>
            </div>
      </div>
    </div>

  </div>
  
  <% } %>
            <div class="card mt-3" style="width: 100%">
              <div class="card-body">
                <%if(REPLY){%>
                <h1>총 댓글 수 : <%=REPLY.reply.length%></h1>
                <hr>
                <%for(let i = 0; i < REPLY.reply.length; i++){%>
                  <p><%=REPLY.reply[i]%></p>
                <%}%>
        <%}%>
              </div>
            </div>

            <form action="/reply/write" method="POST" class="mt-5">
              <div class="mb-3">
                <input type="text" value="<%=POST._id%>" style="display: none;" name="_id">
                <label for="exampleFormControlInput1" class="form-label">댓글</label>
                <input type="text" class="form-control" id="exampleFormControlInput1" placeholder="댓글을 입력해주세요." name="reply">
              </div>
              <button type="submit" class="btn btn-primary mb-5">댓글 작성</button>
            </form>



            <div class="mt-5 mb-5" style="height:100px; text-align: center;">이것은 footer입니다.</div>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
</body>


</html>